import { getCollection } from 'astro:content'

// export const getPageReflection = async (collection: any, moth: any) => {
//   const allPage = (await getCollection(collection))
//     .filter((a: any) => a.data.moth === moth)
//     .sort((a: any, b: any) => a.data.id.length - b.data.id.length)
//   return allPage
// }

